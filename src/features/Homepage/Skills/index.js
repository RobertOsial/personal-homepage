import { List } from "../../../common/List";
import { Section } from "../../../common/Section";

export const Skills = () => {
  return (
    <Section
      title="My skillset includes 🛠️"
      body={<List />}
    />
  );
}